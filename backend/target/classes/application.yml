server:
  port: 8080

app:
  jwt:
    secret: "change-me-change-me-change-me-change-me"
    expiration-ms: 604800000
  uploads-dir: "${user.dir}/uploads"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lms?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: "root"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /api/docs/ui
